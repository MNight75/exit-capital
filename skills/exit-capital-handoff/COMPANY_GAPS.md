# Company Gap Ledger

Current strengths:

- Hermes/Nemotron is project-aware.
- Research Intern works through OpenRouter research model.
- Qdrant writes work.
- Markdown idea vault works.
- Discord HOME is pinned.
- Stripe dry-run skills are installed and live money is locked.
- Red Team Council endpoint exists with five OpenRouter models.

Gaps still present:

- CFO is a board seat, not a separate process.
- Archivist is deterministic, not a summarizing LLM.
- Secretary/Mailman records bus messages but does not yet retry or acknowledge delivery.
- Brand Agent is defined but not wired to image/video generation.
- Seedance or other video provider is not configured.
- Async red-team review is not scheduled.
- Lessons loop exists as policy, but postmortem extraction is not fully automated.
- Agent bus has no GUI inbox.

Recommended next fills:

1. Add GUI inbox for `data/agent-bus/messages.jsonl`.
2. Add `/api/postmortem` for killed/rejected ventures.
3. Add `/api/brand-brief` for Brand Agent.
4. Add async scheduled red-team review only after HOME delivery is stable.
5. Add CFO endpoint before enabling any live Stripe money.
